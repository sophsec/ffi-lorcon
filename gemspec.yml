name: ffi-lorcon
summary: Ruby FFI bindings for Lorcon.
description: Ruby FFI bindings for Lorcon.
license: MIT
authors: Postmodern
email: postmodern.mod3@gmail.com
homepage: https://github.com/sophsec/ffi-lorcon
has_yard: true

requirements: liborcon >= 2.0.0

dependencies:
  ffi: ~> 1.0

development_dependencies:
  rubygems-tasks: ~> 0.1
  rspec: ~> 2.0
  yard: ~> 0.7
